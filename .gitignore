# Dependencias — se reinstalan con npm install, nunca se versionan
node_modules/

# Secretos — nunca, jamás, en ningún caso
.env
.env.local
.env.*.local

# Artefactos de build — se regeneran, no son fuente
build/
dist/
coverage/

# Logs
*.log
npm-debug.log*

# Archivos de sistema operativo
.DS_Store
Thumbs.db